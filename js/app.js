// 1,2... 1,2... this is just a test
var testing = function() {
  console.log('app.js loaded');
};
testing();


// homepage hover animations
$(function() {
  // select work project on hover
  $('.fig').on('hover', function() {
    $(this);
  });

  // on hover, show hidden div and text, animate
  // on hover out, hide div and text, animate

});
